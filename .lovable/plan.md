

# Simplify Category Cards

## Change
Remove the subtitle text from category cards, keeping only the emoji and category name. Make cards more compact.

### File: `src/components/NewProjectDialog.tsx`
- Remove `subtitle` from `CATEGORIES` array
- Remove the `<p>` subtitle line from category card rendering
- Adjust card padding/layout for a cleaner, compact look: emoji on top, name below, centered

