

# Improve Selection Styling & Add Dialog Subtitle

## File: `src/components/NewProjectDialog.tsx`

### Selection styling
Change selected state from current subtle gray to a soft primary tint:
- Selected: `bg-primary/10 border-primary/30` with `text-primary font-medium` on the text
- Unselected: keep current default border

### Subtitle
Add a short description line below "Create New Project" title, e.g. "Set up your product shoot in seconds" in muted text.

